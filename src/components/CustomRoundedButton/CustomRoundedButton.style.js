import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'flex-end',
  },
  icon: {
    width: 45,
    height: 45,
    borderRadius: 100,
    backgroundColor: '#fff',
  },
});
