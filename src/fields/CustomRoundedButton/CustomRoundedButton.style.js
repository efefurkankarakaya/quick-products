import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'flex-end',
  },
  icon: {
    width: 80,
    height: 80,
    borderRadius: 100,
    backgroundColor: '#fff',
  },
});
